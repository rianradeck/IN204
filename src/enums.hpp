/**
 * @brief Simple document to store enumerated constants
 * 
 */

enum PieceKind {
    NONE = 0,
    LINE = 1,
    J = 2,
    L = 3,
    SQUARE = 4,
    T = 5,
    S = 6,
    Z = 7,
    BLOCKED = 8
};

enum Direction {
    UP = 0,
    RIGHT = 1,
    DOWN = 2,
    LEFT = 3
};
